// PUT /close/:logId is handled by logRoutes.js (mounted first in app.js).
// This file is kept as an empty router so app.js mount doesn't break during transition.
const express = require('express');
const router = express.Router();
module.exports = router;
